{
  "name": "citable-scripts",
  "private": true,
  "type": "module",
  "description": "Client library and JS side of the leaf hash, verified against src/Leaf.sol by forge test",
  "dependencies": {
    "merkletreejs": "^0.6.0",
    "viem": "^2.21.0"
  },
  "scripts": {
    "test": "node --test \"test/js/*.test.mjs\""
  },
  "devDependencies": {
    "@xenova/transformers": "^2.17.2"
  }
}
